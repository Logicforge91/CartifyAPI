spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/cartify?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:User@123}
  flyway:
    clean-disabled: false
