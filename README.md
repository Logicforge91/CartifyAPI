# CartifyAPI
CartifyAPI is a RESTful backend service for e-commerce platforms, built with Java and Spring Boot. It handles core commerce operations — product catalog, cart, checkout, and order management — through clean, versioned API endpoints.
